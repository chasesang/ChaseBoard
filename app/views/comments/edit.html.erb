<h1>Edit Comment</h1>
<%= simple_form_for([ @message.team, @comment.message, @comment]) do |f| %>
	<%= f.input :content, label: "Comment" %>
	<%= f.button :submit, :class => 'btn-custom' %>
<% end %>
